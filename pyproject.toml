[project]
name = "mbaxms-class-profile"
version = "0.1.0"
description = "A local, access-controlled MBAxMS class profile directory."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "django>=5.2,<5.3",
    "pillow>=11,<13",
    "python-dotenv>=1.1,<2",
]

[dependency-groups]
dev = []

